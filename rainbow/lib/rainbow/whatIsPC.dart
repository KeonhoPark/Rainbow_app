import 'package:flutter/material.dart';

class WhatIsPC extends StatelessWidget {
  const WhatIsPC({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('What is PC?')),
    );
  }
}
